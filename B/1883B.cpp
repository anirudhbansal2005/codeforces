#include<bits/stdc++.h>
using namespace std;
bool isPalindrome(string s){
    int front = 0, back  = s.size()-1;
    while(front <= back){
        if(z)
    }
}
int main(){
    int t;
    cin >> t;
    for(int i=0; i<t; i++){
        int a, b;
        cin >> a >> b;
        string s;
        cin >> s;
    }
}