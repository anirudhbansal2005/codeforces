#include<bits/stdc++.h>
using namespace std;
int strangePart(vector<int> v){
    vector<int> x;
    for(int i=0; i<v.size(); i++){
        x[i] = 
    }
}
int vecBeauty(int x, vector<int> v, vector<int>& y){
    int temp=0;
    for(int i=0; i<v.size(); i++){
        temp+=v[i]/x;
    }
    y.push_back(temp);
}
int main(){
    int t;
    cin >> t;
    for(int i=0; i<t; i++){
        int n, x;
        vector<int> vec(n);
        vector<int> b;
        for(int j=0; j<n; j++){
            cin >> vec[j];
        }
    }
}